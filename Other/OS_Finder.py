import os
import platform

print(os.name)              # Prints the name of OS
print(platform.system())    # Returns the name of the OS 
print(platform.release())   # Returns the precise OS release date
